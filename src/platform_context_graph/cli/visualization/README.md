# CLI Visualization

HTML graph rendering helpers for CLI output live here.

Keep visualization data shaping, HTML template generation, and rendering utilities separate so the output stays readable and easy to extend.
