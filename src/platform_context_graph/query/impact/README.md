# Query Impact

Impact-path and change-surface query helpers live here.

This package owns the in-memory graph-store logic used to trace resources, explain dependencies, and compute impacted entities.
