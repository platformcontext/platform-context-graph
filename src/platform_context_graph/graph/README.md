# Graph Package

Canonical graph schema and persistence helpers live here.

This package owns:

- schema creation and graph capability boundaries
- graph write helpers and batch orchestration
- graph persistence result types and worker support

If code is about writing or shaping the canonical graph, it belongs here rather
than under `tools/`.
