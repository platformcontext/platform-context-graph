# Parity Closure Matrix

This matrix turns the parity audit into an execution-grade checklist.

Use this page when deciding whether a workstream is ready to split, implement,
or declare complete. A row only moves to `pass` when the Go platform matches
the old Python platform feature for feature for that surface.

## Status Legend

| Status | Meaning |
| --- | --- |
| `pass` | Go-owned and parity-complete |
| `partial` | Go-owned, but feature or operator parity is still incomplete |
| `fail` | required parity behavior is still missing |
| `bounded` | intentionally narrower than Python and must stay documented as a non-goal |

## Branch Baseline

| Workstream | Status | Current truth | Approval bar |
| --- | --- | --- | --- |
| Runtime ownership | `pass` | Long-running and one-shot services are Go-owned end to end | keep Go-only ownership intact |
| Deployment ownership | `pass` | Dockerfile, Compose, and Helm run the Go platform | keep docs and validation aligned |
| Python normal-path ownership | `pass` | No normal-path Python runtime code remains | do not reintroduce mixed-runtime ownership |
| Feature-for-feature parity | `partial` | The branch is Go-owned, typed relationship fidelity is proven end to end, repo-local GitHub Actions workflow artifacts now surface reusable workflow repositories, explicit checkout repositories, explicit workflow-input repositories, run-command counts, derived delivery-command families, and repo-local delivery paths for Terraform `-chdir`, Terragrunt working directories, Helm chart paths, kubectl manifests, Ansible playbooks/inventories/vars, and Compose file references on the Go read path, shared-infra deployment tracing now has live compose-backed HTTP proof, and the projector/reducer runtime now uses explicit dead-letter queue semantics with replay support for both new `dead_letter` rows and legacy `failed` rows. The remaining parity gap is narrower: canonical promotion for repo-local workflow hints plus unresolved IaC helper families, not missing queue ownership | only mark `pass` when implementation, proof, and docs all agree |

## Operator And Runtime Contract Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| `pcg serve start` contract | `pass` | `pcg serve start` is now documented and shipped as the HTTP API convenience process, while MCP stays on `pcg mcp start` | keep command behavior and docs aligned exactly | focused Go CLI tests, docs check | validation sweep only |
| API admin route mounting | `pass` | The shipped Go API exposes the public `/api/v0/admin/*` control surface, while the runtime still mounts the shared `/healthz`, `/readyz`, `/admin/status`, and `/metrics` contract on the same listener | keep the public admin routes and runtime admin surface aligned in code, OpenAPI, and docs | `go test` for `./internal/query ./cmd/api`, OpenAPI diff, docs build | validation sweep only |
| Status endpoint breadth | `pass` | The API runtime now mounts the shared `/healthz`, `/readyz`, `/admin/status`, and `/metrics` surface, while public ingester status is available on canonical `/api/v0/status/ingesters*` routes with legacy `GET /api/v0/ingesters*` aliases | keep the mounted runtime surface and public status aliases documented and tested | `go test` for `./internal/status ./internal/query ./cmd/api`, docs build | validation sweep only |
| Run-scoped coverage endpoints | `bounded` | The shipped Go contract is checkpoint status plus repository-scoped coverage only: `/api/v0/status/index`, `/api/v0/index-status`, and `/api/v0/repositories/{repo_id}/coverage`. Run-scoped completeness routes are intentionally not part of the public OpenAPI contract on this branch | docs and OpenAPI keep that bounded contract explicit without implying `/api/v0/index-runs/*` support | OpenAPI verification, docs build | no implementation slice unless the product contract changes |
| CLI behavior breadth | `pass` | The supported Go CLI contract is now explicit in both code and docs: historical commands are either supported, deprecated with guidance, or intentionally removed with compatibility errors instead of silently drifting from the Python-era UX | keep command metadata, docs, and focused `go/cmd/pcg` tests aligned | focused `go/cmd/pcg` tests and smoke checks | validation sweep only |
| Queue lease recovery and ack visibility | `pass` | projector/reducer runtime ownership is Go-only, lease-reclaim predicates plus stalled-overdue-claim status proof are pinned by tests, terminal queue rows now land in explicit `dead_letter` state, replay accepts both `dead_letter` and legacy `failed`, admin dead-letter/skip actions write the same terminal state, runtime metrics have direct proof for dead-letter, failed-legacy, and overdue queue gauges, and compose-backed proof now covers incremental-refresh retry/supersession, projector ack visibility, and reducer dead-letter handling end to end | queue work can be reclaimed safely, stalled health is explicit, poison work is surfaced clearly, and telemetry/logging show retry, ack, and dead-letter behavior without manual inference | `go test` for `./internal/projector ./internal/reducer ./internal/runtime ./internal/status ./internal/storage/postgres ./internal/telemetry` plus compose-backed runtime proof | validation sweep only |

## Parser And Graph-Surface Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| SQL core parsing | `pass` | Core SQL object and DDL parity is now closed: Go covers `CREATE TABLE IF NOT EXISTS`, `CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS`, `CREATE OR REPLACE FUNCTION` bodies, tagged dollar-quoted procedural bodies with `LANGUAGE` before or after `AS`, `CREATE OR REPLACE VIEW`, `CREATE PROCEDURE`, `CREATE MATERIALIZED VIEW`, legacy `EXECUTE PROCEDURE` trigger wiring, and fixture-backed `ALTER TABLE ... ADD COLUMN ...` column materialization, including bounded multi-clause `ADD COLUMN` normalization, with parser plus content/query fallback proof | keep the proof current and keep broader dialect-specific procedural forms plus wider ALTER/DDL mutation normalization documented as bounded non-goals rather than parity gaps | focused parser/content/query tests | move to validation sweep only |
| SQL/dbt lineage | `pass` | Python-era feature parity is met: the Go parser now preserves lineage through safe wrappers, lineage-preserving macro wrappers that still expose real source refs, `concat_ws`, `md5`, fixture-backed wildcard-plus-`coalesce(...)` lineage, top-level Jinja wrappers around those same safe forms, and checked-in materialization/query proof for `AnalyticsModel` plus `DataAsset` content entities. The still-unresolved dbt cases are historical Python limitations or bounded non-goals rather than missing Go parity features | compiled dbt lineage survives parse, materialization, and query proof | parser tests, materialization/query proof, broader real-repo proof as validation follow-on | move to validation sweep, not parity implementation |
| Canonical code-call materialization | `pass` | the collector emits explicit `code_call_materialization` follow-ups, the reducer owns repo-scoped retract-and-rewrite of canonical `CALLS`/`REFERENCES`/`USES_METACLASS` edges for SCIP-backed parser facts plus generic `function_calls`, Neo4j persistence stays on the canonical shared-write path, and the public Go query contract now proves exact `code/relationships`, decorator-aware `dead-code`, and `code/call-chain` traversal over persisted `CALLS` edges with exact-name or entity-id plus repo-scoped lookup instead of legacy fuzzy `CALLS_FUNCTION` assumptions. Primary non-SCIP families preserve richer generic call metadata (`full_name`, JSX/function kind where available, receiver type where available), and reducer proof covers same-file plus cross-file matching for JS/TS/TSX import-driven calls, Python module/`from` imports, exact-qualified Swift/Ruby/Elixir/PHP static-style calls, Kotlin safe-call receiver chains, PHP nullsafe receiver chains, and repo-unique cross-file bare-name calls across the remaining parser-first long-tail families (C, C++, C#, Java, Scala, Kotlin, Rust, Dart, Haskell, Perl). | required language families persist canonical call edges through parser, collector, reducer, Neo4j, and query proof without Python fallback | parser tests, collector/reducer tests, Neo4j persistence tests, query contract tests | closed |
| JavaScript graph parity | `pass` | no remaining graph-first JavaScript promotion gap: docstrings and method-kind metadata are extracted and materialized, generator functions carry `semantic_kind=generator`, compile-time constant computed class-member expressions normalize to names, static CommonJS `require()` imports are tracked, the graph-backed `language-query` path returns those fields directly from graph rows, `code/search`, `entities/resolve`, `code/call-chain`, `dead-code`, `code/relationships`, and `code/complexity` preserve graph-backed JavaScript semantics when the row already carries them, `entities/{entity_id}/context` promotes graph-backed JavaScript metadata directly into `metadata`, `semantic_summary`, `semantic_profile`, and `story`, shared query outputs emit semantic summaries and a structured `semantic_profile`, JavaScript method-kind rows get a dedicated `javascript_method` surface kind, and repository stories expose semantic coverage via `semantic_overview` | JS docstrings, method metadata, and generator semantics persist and query correctly across parser, graph-backed language-query, search, resolve, call-chain, dead-code, relationships, complexity, query/context surfaces, and normal story surfaces | parser tests plus query proof | validation sweep only |
| TypeScript graph parity | `pass` | no remaining graph-first TypeScript promotion gap: decorator and generic metadata are preserved, canonical `Function`, `Class`, and `Interface` rows now persist `decorators` and `type_parameters` through the Go projector/reducer/Neo4j path, `TypeAlias` persists as a first-class graph entity through the Go projector/reducer/Neo4j path, mapped types and conditional types carry dedicated `type_alias_kind` semantics on the normal Go parser/content/query path, namespace declarations materialize as first-class `Module` graph entities with `module_kind=namespace`, same-file declaration merging survives the normal Go parser/content/query/story path and persists through the Go semantic-entity materialization path, the shared graph-backed `language-query` path projects namespace and declaration-merging metadata directly from matching graph rows, content-backed entities still participate in normal entity resolve/context when the graph is empty, the normal `code/search` fallback still searches entity names as well as source text, graph-backed `language-query`, `code/search`, `dead-code`, `code/relationships`, `code/complexity`, `entities/resolve`, and entity-context results enrich matching rows with metadata, `language-query`, `code/search`, plus entity-context emit semantic summaries and a structured `semantic_profile`, entity-context also emits a first-class `story`, and repository stories expose semantic coverage via `semantic_overview` | those semantics persist and query correctly across parser, graph-backed `TypeAlias` rows, graph-backed `Module` rows, canonical function/class-family graph rows, graph-backed `language-query` metadata projection, content fallback, and normal query surfaces | parser tests, reducer tests, graph tests, API/MCP proof | validation sweep only |
| TSX graph parity | `pass` | no remaining graph-first TSX promotion gap: JSX tag usage is captured, `TypeAlias` and `Component` persist as first-class graph entities through the Go projector/reducer/Neo4j path, TSX fragment shorthand survives on function/component entities as `jsx_fragment_shorthand`, `as ComponentType<...>` narrowing plus direct type-annotation forms such as `const Wrapped: CT<Props> = ...` survive on variable entities as `component_type_assertion`, `React.FC(...)`-style and namespace-qualified `React.FunctionComponent(...)` assertions also survive on variable entities as `component_type_assertion`, `memo(...)`, `forwardRef(...)`, `lazy(...)`, and parenthesized wrapper calls survive on component entities as `component_wrapper_kind`, the shared graph-backed `language-query` path projects fragment shorthand, component-assertion, and wrapper metadata directly from matching graph rows, content-backed entities still participate in normal entity resolve/context when the graph is empty, the normal `code/search` fallback still searches entity names as well as source text, `code/language-query` accepts direct `tsx` requests instead of requiring the broader `typescript` alias, canonical graph writes persist JSX component usage as first-class `REFERENCES` edges, the query layer still normalizes older graph-backed `CALLS` rows with `call_kind=jsx_component` for compatibility, shared query outputs emit semantic summaries plus a structured `semantic_profile`, entity-context emits a first-class `story`, and repository stories expose semantic coverage via `semantic_overview` | JSX component semantics, fragment shorthand, `ComponentType` narrowing, and wrapper metadata persist through parser, projector/reducer, Neo4j, graph-backed query, entity-context/story, and repository-story surfaces | parser tests, reducer tests, graph tests, API/MCP proof | validation sweep only |
| Python graph parity | `pass` | decorators and async flags are extracted and materialized, type annotations are queryable through Go content-backed APIs, function-signature and annotated-assignment type annotations now materialize as first-class `TypeAnnotation` entities, identifier-assigned, attribute-assigned, and anonymous inline lambdas now materialize as function entities with `semantic_kind=lambda`, generator functions now materialize as function entities with `semantic_kind=generator`, Python semantic `Function` entities carrying decorators, async flags, generator semantics, or lambda semantics now also persist through the semantic-entity projector/reducer/Neo4j path, Python class entities now also preserve `metaclass` metadata and persist `USES_METACLASS` relationships, Python module docstrings now materialize as first-class `Module` entities, content-backed entities now participate in normal entity resolve/context, the normal `code/search` fallback can now search content-backed entity names as well as source text, content-backed decorator-only, async-only, decorated-async, assignment-annotation, and parameter-annotation rows now have explicit resolve/context proof, and graph-backed `language-query`, `code/search`, `entities/resolve`, `dead-code`, `code/complexity`, plus entity-context/story now project Python semantics directly from graph rows with a first-class `python_semantics` bundle and corrected surface-kind priority | those semantics persist and query correctly across parser, semantic-entity materialization, graph-backed language-query, content-backed resolve/context fallback, search, dead-code, complexity, and normal query/context/story surfaces | parser tests, graph tests, API/MCP proof | validation sweep only |
| Java graph parity | `pass` | applied annotation usage now persists as first-class `Annotation` graph nodes through the Go projector/reducer/Neo4j path, stays graph-first on the normal `code/language-query` surface, falls back to content when the graph is empty, and normal resolve/context/story responses still expose humanized semantic summaries plus an `applied_annotation` semantic profile | keep parser, materialization, query, and docs proof aligned | parser tests, reducer tests, Neo4j writer tests, query proof | validation sweep only |
| Kotlin graph parity | `pass` | typed locals, casts, direct cast expressions, primary-constructor properties, `this`/object/companion-object receivers, smart casts, safe-call flows, `apply`/`also` scope-function-preserved assignments, lazy delegates, same-file and sibling-file function-return aliases, and package-aware cross-file function-return chains now all materialize canonical Kotlin `CALLS` edges through the Go parser/reducer path, and the public Go `code/relationships` surface has checked-in proof for those receiver families | keep parser, reducer, query proof, and language-page evidence aligned | parser tests, reducer tests, query proof | validation sweep only |
| PHP graph parity | `pass` | typed `$this` receivers, aliased `new` receivers, same-file free-function return aliases and call chains, method-return property chains, static-property receiver chains, parent/static property access chains, imported alias/static alias chains, cross-file return-type aliases, chained static factory returns, anonymous-class receivers, nullsafe receivers, and direct `self`/`static` instantiation rows now all materialize canonical PHP edges through the Go parser/reducer path, and the public Go `code/relationships` surface has checked-in proof for those bounded object-call families | keep parser, reducer, query proof, and language-page evidence aligned | parser tests, reducer tests, query proof | validation sweep only |
| C graph parity | `pass` | typedefs now persist as first-class `Typedef` graph nodes through the Go projector/reducer/Neo4j path, prefer the graph-first normal language-query surface, fall back to content when the graph is empty, and still emit semantic summaries in normal query/context responses | keep parser, materialization, query, and docs proof aligned | parser tests, reducer tests, Neo4j writer tests, query proof | validation sweep only |
| Rust graph parity | `pass` | impl blocks now persist as `ImplBlock` graph nodes, preserve bounded lifetime metadata on function and impl signatures, are queryable through `code/language-query`, surface semantic summaries through normal `entities/resolve` and entity-context fallbacks, keep `kind`/`trait`/`target`/`semantic_summary` metadata on the normal query path, and now expose exact impl ownership through first-class graph `CONTAINS` edges to Rust functions while canonical call materialization honors exact `impl_context`-qualified Rust methods | impl ownership, bounded lifetime metadata, and relationships persist and query correctly on the normal Go query path | parser tests plus query proof | validation sweep only |
| Elixir graph parity | `pass` | guards and module attributes now persist as first-class graph-backed `Function` and `Variable` semantics through the Go semantic-entity materialization path, surface in semantic summaries, structured `semantic_profile` payloads, and repository-story semantic counts on normal query/context responses, and resolve directly through the shared graph-backed `language-query` path; `defprotocol` and `defimpl` also persist as first-class `Protocol` and `ProtocolImplementation` graph nodes, while reducer-owned same-file plus cross-file call materialization stays exact-only for qualified Elixir call metadata | Elixir guard, module-attribute, protocol, and protocol-implementation semantics persist and query correctly through parser, semantic-entity materialization, graph-backed language-query, and normal query/context/story surfaces | parser tests, reducer tests, graph tests, API/MCP proof | validation sweep only |

## IaC And Deployment Semantics Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| Terraform parity | `pass` | Python-era parser/runtime parity is met, and Go now exceeds that baseline with first-class `terraform {}` block materialization plus raw `count`/`for_each` metadata on resource rows. Provider-schema evidence is now also proven through the Go ingestion transaction: repository facts are loaded into a catalog, Terraform fact batches are matched against it, and schema-driven evidence persists to `relationship_evidence_facts` before projector work is enqueued. `count`/`for_each` expansion and `dynamic` traversal remain net-new future enhancements rather than parity blockers because Python also lacked them | Terraform structural metadata and provider-schema behavior match the Python-era platform, with query proof for the Go-only `TerraformBlock` surface and ingestion-boundary proof for schema-driven relationship evidence | parser tests, relationship tests, ingestion proof, query proof | shared HCL wave |
| Terragrunt parity | `pass` | core parsing is Go-owned, dependency/local/input entities are queryable, `terraform.source` now also materializes through the normal `TerraformModule` surface, and parser helper metadata now preserves joined sidecar paths such as `join("/", [path_relative_to_include(), "global.yaml"])`, `join("/", [get_terragrunt_dir(), "templates/runtime.json"])`, `join("/", [get_parent_terragrunt_dir(), "templates/runtime.json"])`, `join("/", [get_repo_root(), "config/runtime.yaml"])`, and `join("/", [path.module, "templates/runtime.json"])`, plus local-backed interpolation and nested helper-built paths such as `join("/", [get_repo_root(), "accounts/${local.account_name}/account.yaml"])` and `lookup(..., "${path.module}/batch/${local.container_template}")`, in `local_config_asset_paths` | Terragrunt preserves its current Go-owned entities and the historical module-source plus helper-path semantics the Python platform exposed | parser tests, query proof, relationship tests | shared HCL wave |
| Kubernetes parity | `pass` | parser payload parity is met and now exceeds Python with `qualified_name`, and the Python-era same-name/same-namespace `Service -> Deployment` `SELECTS` heuristic now survives on the Go-owned content/query path. Real `selector` / `matchLabels` resolution and CRD schema awareness were historical Python limitations too, so they remain bounded non-goals rather than parity blockers | Kubernetes parser payloads plus the historical `SELECTS` heuristic survive parse, relationships, and query proof | parser tests, relationship tests, query proof | validation sweep only |
| ArgoCD parity | `pass` | parser payload parity is met and now exceeds Python with sync-policy normalization plus explicit ApplicationSet generator/template source split, Go emits discovery, deploy-source, and destination-platform evidence for ArgoCD Applications/ApplicationSets, the normal Go entity resolve/context fallback now surfaces that chain through semantic summaries plus synthesized `DISCOVERS_CONFIG_IN`, `DEPLOYS_FROM`, and `RUNS_ON` relationships, repository stories now expose structured deployment/gitops/support overviews for that same evidence lane, and the Go-owned `trace_deployment_chain` MCP/API path now returns story-first deployment traces with `deployment_overview`, `deployment_sources`, `cloud_resources`, `k8s_resources`, `image_refs`, `k8s_relationships`, `deployment_facts`, `deployment_fact_summary`, `controller_driven_paths`, `delivery_paths`, and concrete ArgoCD controller entities in `controller_overview.entities` | ArgoCD parser payloads plus the historical Application/ApplicationSet evidence chain survive parse, relationships, entity-context fallback, repository-story surfacing, richer controller-overview deployment-trace proof, and public query proof | parser tests, relationship tests, query proof | validation sweep only |
| CloudFormation parity | `pass` | YAML and JSON templates now share the same parser materialization, including `file_format`, cross-stack import/export buckets, first-class condition definitions, evaluated condition results when expressions are template-local, and nested-stack `template_url` metadata that now also resolves obvious repo-local nested-stack targets on the Go entity-context path | JSON and YAML templates reach the same materialization and query-proof bar for the currently modeled CloudFormation semantics | parser tests plus end-to-end proof | validation sweep only |
| Kustomize parity | `pass` | parser payload parity is met and now exceeds Python with first-class sorted `bases`, inline patch-target extraction, normalized `resource_refs`/`helm_refs`/`image_refs`, and typed Kustomize evidence for resources vs Helm vs images. The historical patch-link heuristic plus typed `DEPLOYS_FROM` query fallback now survive on the Go-owned query path | Kustomize parser payloads plus the historical typed evidence/link behavior survive parse, relationships, and normal query proof | parser tests, relationship tests, query proof | validation sweep only |
| Generic JSON | `bounded` | arbitrary JSON is intentionally quiet to avoid graph noise | accepted bounded behavior is documented and unchanged | docs build only unless scope changes | do not schedule unless requirement changes |
| Typed relationship fidelity | `pass` | reducer intent generation preserves typed repo relationships for the same source/target pair instead of collapsing them onto a generic repo-pair key, reducer normalization keeps canonical repo ids before edge writes, reducer-to-Neo4j dispatch preserves `evidence_type`, query/story outputs classify controller-driven, workflow-driven, and IaC-driven relationship summaries, and the compose-backed relationship-platform proof now passes against the Go-owned runtime path | keep typed repo edges, evidence families, and story/query shaping aligned without lossy fallback | `go test` for `./internal/relationships ./internal/reducer ./internal/query ./internal/storage/neo4j` plus `./scripts/verify_relationship_platform_compose.sh` | validation sweep only |
| Workflow and automation relationship mapping | `partial` | GitHub Actions reusable workflows, explicit cross-repo checkout, explicit repo-bearing workflow inputs, step-level action repositories, Jenkins shared-library refs and GitHub repository URLs, and Ansible inventories/playbooks/roles/task entrypoints now produce canonical repo evidence or query-visible controller artifacts on the Go path. Repo-local GitHub Actions workflow files now survive the normal Go ingestion path instead of being skipped as hidden paths, the relationship-platform compose proof now verifies canonical `DEPLOYS_FROM` and `DISCOVERS_CONFIG_IN` edges from a real workflow file, and query/story proof now locks down repo-local workflow files as read-side delivery artifacts with local reusable workflow paths, reusable-workflow repositories, explicit checkout repositories, step-level action repositories, explicit workflow-input repositories, list-form `workflow_input_repositories`, run-command counts, derived delivery-command families, repo-local delivery paths from Terraform `-chdir`, Terragrunt working directories, Helm chart paths, kubectl manifests, Ansible playbooks/inventories/vars, and Compose file refs, trigger events, workflow inputs, actual gating-condition text, matrix metadata, permissions scopes, concurrency groups, job environments, job timeout metadata, and `needs` details, without inventing fake canonical repo edges from local `uses: ./.github/workflows/...` calls. GitHub Action step repositories now resolve as truthful canonical `DEPENDS_ON` evidence rather than being overstated as deploy-source or config-discovery edges, and `uses:` action references remain distinct from real shell-command delivery families on the Go read path. Jenkins canonical evidence now preserves the parser-proven controller metadata bundle in evidence details, and the real `ansible_jenkins_automation` fixture now locks down shared-library, explicit GitHub repo, root-level playbook, and controller-artifact behavior on the Go path. The remaining gap is canonical promotion for repo-local workflow hints beyond the current explicit repo-bearing evidence and read-side surfacing | workflow families emit canonical evidence and query-visible relationships with docs and tests to match | parser tests, relationship tests, query proof, compose-backed relationship proof | parser-family chunk |
| Docker and compose relationship mapping | `partial` | Docker Compose build contexts, including shorthand `build: ../repo`, and image refs now produce canonical deploy-source evidence in focused relationship proof, explicit `depends_on` service names now produce canonical dependency evidence when they resolve truthfully through the repo catalog, Dockerfile source labels now produce truthful canonical `DEPLOYS_FROM` evidence, and repo context/story now surface runtime artifacts for both Compose services and Dockerfile stages, including overlay-style Compose filenames plus Compose `command`/`entrypoint`, `env_file`, `configs`, `secrets`, and Dockerfile `CMD` visibility on the normal Go read path. Repo-local Compose `env_file` entries plus top-level `configs.file` and `secrets.file` definitions now also flow into `config_paths`, `shared_config_paths`, `delivery_paths`, and topology-story config provenance instead of staying runtime-only metadata. The relationship-platform compose proof now verifies the live Go stack end to end for the Compose deploy-source lane as currently materialized, `depends_on` edges, and the real `service-worker-jobs/Dockerfile` runtime artifact lane, while focused relationship tests keep `docker_compose_build_context` and `docker_compose_image` evidence pinned independently. Plain Docker base images and stage aliases still stay bounded read-side signals rather than repo edges, and that negative edge case remains pinned by focused relationship proof | image/build/runtime relationships survive parse, graph materialization, and query proof without inventing false Docker repo edges | parser tests, relationship tests, query proof, compose-backed relationship proof | parser-family chunk |
| Terraform variable-file relationship mapping | `pass` | `.tfvars` and `.tfvars.json` now have relationship extraction plus reducer and query proof on the normal Go path | keep variable-file evidence in canonical relationship resolution and query surfacing | parser tests, relationship tests, reducer proof, query proof | validation sweep only |
| Shared-config provenance | `partial` | the Go normal read path now extracts Kustomize policy-document SSM config paths from related repos, preserves Terragrunt `dependency.config_path` rows, surfaces Terragrunt `read_terragrunt_config` / `include` / `find_in_parent_folders` config references including the default no-argument parent `terragrunt.hcl` form, repo-local Terraform/HCL literal-path `file()` / `templatefile()` assets plus local-variable-backed forms such as `file(local.config_file_path)` and `templatefile(local.lifecycle_template, ...)`, lookup-backed local-variable config assets such as `file(local.template)` where `local.template` comes from `lookup(..., "${path.module}/templates/user_data.tpl")`, helper-composed Terraform asset paths such as `templatefile(join(\"\", [path.module, \"/specs/AppSpec.yaml\"]), ...)` and `templatefile(join(\"/\", [path.module, \"specs/AppSpec.yaml\"]), ...)`, nested local interpolation defaults such as `templatefile(lookup(..., \"${path.module}/batch/${local.container_template}\"), ...)`, `*.tfvars` / `*.tfvars.json` files, local Terraform `module.source = \"./...\"` asset paths, helper-built Terragrunt `terraform.source = \"${get_repo_root()}/...\"` module paths, joined Terragrunt sidecar helper paths such as `join(\"/\", [path_relative_to_include(), \"global.yaml\"])`, `join(\"/\", [get_terragrunt_dir(), \"templates/runtime.json\"])`, `join(\"/\", [get_parent_terragrunt_dir(), \"templates/runtime.json\"])`, `join(\"/\", [get_repo_root(), \"config/runtime.yaml\"])`, and `join(\"/\", [path.module, \"templates/runtime.json\"])`, plus local-backed interpolation and nested helper-built Terragrunt assets such as `join(\"/\", [get_repo_root(), \"accounts/${local.account_name}/account.yaml\"])` and `templatefile(lookup(..., \"${path.module}/batch/${local.container_template}\"), ...)`, the service-level `split(\"/\", path_relative_to_include(...))` Terragrunt pattern in both named and unnamed helper forms that now recovers repo-local `account.yaml`, `region.yaml`, and `vpc.yaml` assets without promoting remote-state keys into fake config provenance, and repo-local Docker Compose `env_file` entries plus top-level `configs.file` and `secrets.file` definitions. The Go path then groups `deployment_artifacts.config_paths` into `shared_config_paths` and also preserves single-source config provenance in `delivery_paths` plus `topology_story`. Repo-bearing Terragrunt helper/config paths now also survive on the canonical evidence path as typed `DISCOVERS_CONFIG_IN` evidence with helper-kind details instead of existing only as read-side/query-only provenance. Repository query surfaces now also count `RUNS_ON` alongside the other canonical repo-scoped relationship families instead of dropping runtime provenance from dependency views, and the live compose proof now exercises `POST /api/v0/impact/trace-deployment-chain` so runtime-chain validation no longer depends only on repository context summaries. The remaining work is the broader helper-built path expressions that still cannot be proven exactly from the checked-in source, not basic local config-asset visibility or missing trace proof | shared config provenance and runtime provenance are visible in repository context, story, and trace outputs without inventing stronger deployment semantics, and broader IaC config assets are either implemented or explicitly bounded | query proof, story proof, compose-backed trace proof, docs truth check | parser-family chunk |
| Ansible relationship mapping | `pass` | Ansible is now a first-class artifact family on the Go path: inventories, playbooks, `group_vars` / `host_vars`, role roots, and task entrypoints are classified explicitly; playbooks that truthfully resolve to repo evidence emit canonical role-reference relationships; and Jenkins-driven controller artifacts plus repository deployment/story summaries now carry adjacent Ansible inventories, vars, and task entrypoints instead of dropping them on the floor. Root-level playbooks such as `deploy.yml` now also participate in canonical role-reference discovery when their content truthfully resolves to a repo, which is locked down by the real `ansible_jenkins_automation` fixture corpus | inventories, playbooks, vars, role references, and task entrypoints are queryable and documented with fixture-backed proof | parser tests, relationship tests, query proof | validation sweep only |

## Relationship Verb Coverage Matrix

This is the branch-close bar for relationship parity. It is keyed to the
historical relationship README, relationship-graph guides, and fixture-corpus
design rather than to parser output alone.

| Family or scenario | Expected verbs | Current Go truth | Remaining gap | Required proof |
| --- | --- | --- | --- | --- |
| Dual-runtime service topology | `DEFINES`, `INSTANCE_OF`, `RUNS_ON`, `WORKLOAD_DEPENDS_ON` | workload-instance runtime shape is now the canonical query path | workload dependency proof still needs to stay explicit in stories and traces | reducer/query tests plus deployment-trace proof |
| Shared infrastructure and runtime stacks | `PROVISIONS_PLATFORM`, `PROVISIONS_DEPENDENCY_FOR`, `DEPENDS_ON` | typed repo-edge preservation is in place, reducer intent rows now keep distinct typed repo relationships for the same source/target pair, runtime stories now follow workload instances, and the Go deployment trace surface now pins controller-driven provenance plus delivery-path entries for deployment sources, cloud resources, k8s resources, image refs, and k8s relationships with live compose-backed HTTP proof | keep the explicit trace regression in place as broader IaC helper coverage evolves | relationship tests plus compose-backed trace proof |
| ArgoCD delivery path | `DISCOVERS_CONFIG_IN`, `DEPLOYS_FROM`, `RUNS_ON` | Go-owned and query-visible | keep covered in regression proof | parser, query, and trace tests |
| Helm and Kustomize deployment chain | `DEPLOYS_FROM` | Go-owned and query-visible | keep chained proof aligned with ArgoCD and repo stories | parser and query tests |
| Terraform and Terragrunt runtime context | `PROVISIONS_PLATFORM`, `PROVISIONS_DEPENDENCY_FOR`, `RUNS_ON` | Go-owned, including provider-schema-backed evidence and verified Terraform variable-file promotion on the normal path | broader mixed-corpus validation remains the follow-on, not a missing parity feature | parser, relationships, ingestion, reducer, query, and end-to-end proof |
| GitHub Actions delivery automation | `DEPLOYMENT_SOURCE` and supporting `DEPLOYS_FROM`, `DISCOVERS_CONFIG_IN`, or `DEPENDS_ON` where the evidence is strong enough | reusable workflow refs now emit `DEPLOYS_FROM`, explicit cross-repo checkout now emits `DISCOVERS_CONFIG_IN`, explicit repo-bearing workflow inputs such as `automation-repo`, `workflow_input_repository`, and list-form `workflow_input_repositories` values from either source or metadata now also emit `DISCOVERS_CONFIG_IN`, step-level action repositories such as `hashicorp/setup-terraform@v3` now emit truthful canonical `DEPENDS_ON`, and the query path has direct proof for those explicit cases from source or metadata. Repo-local workflow files now survive normal Go ingestion, query/story proof pins them as read-side delivery artifacts, and those workflow artifacts now also preserve local reusable workflow paths, reusable-workflow repositories, explicit checkout repositories, step-level action repositories, explicit workflow-input repositories, list-form `workflow_input_repositories`, run-command counts, derived delivery-command families, repo-local delivery paths from Terraform `-chdir`, Terragrunt working directories, Helm chart paths, kubectl manifests, Ansible playbooks/inventories/vars, and Compose file refs, trigger events, workflow inputs, matrix metadata, permissions scopes, concurrency groups, job environments, and job timeout metadata on the read path. Local reusable-workflow calls still stay non-canonical unless they carry explicit repo-bearing evidence, and canonical promotion for repo-local workflow hints beyond current read-side surfacing remains open | parser, relationships, query, and compose-backed proof |
| Jenkins / Groovy controller-driven delivery | controller-driven derived delivery path backed by truthful lower-layer evidence | explicit shared-library refs, including `library(...)` step forms, and explicit GitHub repository URLs emit canonical repo evidence, those evidence facts also preserve parser-proven controller metadata such as pipeline calls, entry points, shell commands, Ansible playbook hints, and config/pre-deploy flags, and the real `ansible_jenkins_automation` fixture now locks that behavior through parser, relationships, query, and story coverage | keep regression proof in place as workflow families evolve | parser, relationships, query, and story tests |
| Ansible automation path | controller-driven derived delivery path plus any truthful deploy/config verbs supported by the evidence | parser and query paths classify inventories, vars, roles, playbooks, and task entrypoints explicitly, only playbooks that truthfully resolve to repo evidence emit canonical role-reference evidence, inventories/vars/runtime hints stay read-side until they resolve to repo evidence, and the fixture corpus now proves root-level playbook role references plus adjacent controller summaries on the Go path | keep regression proof in place as automation families evolve | parser, relationships, and query tests |
| Docker and Docker Compose runtime/deploy hints | `DEPLOYMENT_SOURCE`, `DEPENDS_ON`, and supporting deploy/runtime evidence where truthful | Docker Compose build contexts, including shorthand `build: ../repo`, and image refs now emit canonical `DEPLOYS_FROM` evidence in focused relationship proof, explicit `depends_on` service names now emit canonical `DEPENDS_ON` evidence when they resolve through the repo catalog, Dockerfile source labels now emit truthful `DEPLOYS_FROM` evidence, and repo context/story now surface runtime artifacts for Compose services plus Dockerfile stages, including overlay Compose filenames, Compose `command`/`entrypoint`, `env_file`, `configs`, `secrets`, and Dockerfile `CMD`. Repo-local Compose `env_file` entries plus top-level `configs.file` and `secrets.file` definitions now also survive as query-visible config provenance through `config_paths`, `shared_config_paths`, and delivery-path shaping. The compose-backed relationship-platform proof now verifies the live Go stack for the current Compose deploy-source lane, the dependency lane, and the real `service-worker-jobs/Dockerfile` runtime artifact lane, while focused relationship proof keeps `docker_compose_build_context` and `docker_compose_image` evidence pinned independently | broader Docker coverage beyond the current explicit source-label and runtime-artifact cases still open | parser, relationships, query, and compose-backed proof |

## Query-Surface And Documentation Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| API/MCP query surfacing | `partial` | the broad query contract is Go-owned and mostly proven, and repository context/story outputs now have direct proof for typed relationship overviews plus controller-driven, workflow-driven, and IaC-driven relationship narratives. The Go query tests now pin the controller/workflow partition itself, so the remaining gap is broader end-to-end relationship-family closure rather than missing surface proof for those lanes | only return this row to `pass` when the open relationship-family rows are closed and verified | API tests, MCP proof, relationship query proof | final lock sweep |
| Language pages | `partial` | many language pages are current, but the relationship-heavy workflow/IaC pages still need updates as the hardening work lands | docs must match the actual parser and relationship surfaces, not the hoped-for end state | docs build | update as chunks land |
| Support maturity matrix | `pass` | the matrix now matches the parity audit: SQL real-repo and end-to-end moved from `partial` to `supported`, and no matrix row contradicts the parity closure state | keep the matrix aligned with the parity audit | docs build | closed |
| Roadmap and architecture docs | `partial` | these docs still need to reflect that runtime ownership is complete but relationship hardening and parser-family promotion are still open | branch-truth docs no longer claim full parity before the hardening work is done | docs build | docs truth-reset chunk |

## Suggested Execution Waves

| Wave | Includes | Why this grouping works |
| --- | --- | --- |
| Wave 0 | docs truth reset | remove stale “parity complete” claims before landing more code |
| Wave 1 | queue lease recovery and ack observability | hardens projector/reducer correctness and telemetry |
| Wave 2 | ArgoCD `RUNS_ON` and shared-infra trace proof | keeps runtime-chain and trace validation explicit now that typed relationship fidelity is closed |
| Wave 3 | workflow and IaC parser-family promotion | closes GitHub Actions, Jenkins/Groovy, Ansible, Docker, and Compose relationship gaps |
| Wave 4 | docs lock, validation, and evidence refresh | closes the branch honestly after the implementation work lands |

## Approval Gate

Use this matrix to answer three questions before execution starts:

1. Which rows are required for merge on this branch?
2. Which rows can run in parallel without stepping on each other?
3. Which rows are truly bounded non-goals versus unfinished parity?

Do not treat a row as complete because the parser extracts it. A row is only
complete when persistence, query surfacing, tests, end-to-end proof, and docs
all agree.

For the final branch closeout record, use
[merge-readiness-signoff.md](merge-readiness-signoff.md).
