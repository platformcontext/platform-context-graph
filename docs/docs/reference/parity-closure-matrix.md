# Parity Closure Matrix

This matrix turns the parity audit into an execution-grade checklist.

Use this page when deciding whether a workstream is ready to split, implement,
or declare complete. A row only moves to `pass` when the Go platform matches
the old Python platform feature for feature for that surface.

## Status Legend

| Status | Meaning |
| --- | --- |
| `pass` | Go-owned and parity-complete |
| `partial` | Go-owned, but feature or operator parity is still incomplete |
| `fail` | required parity behavior is still missing |
| `bounded` | intentionally narrower than Python and must stay documented as a non-goal |

## Branch Baseline

| Workstream | Status | Current truth | Approval bar |
| --- | --- | --- | --- |
| Runtime ownership | `pass` | Long-running and one-shot services are Go-owned end to end | keep Go-only ownership intact |
| Deployment ownership | `pass` | Dockerfile, Compose, and Helm run the Go platform | keep docs and validation aligned |
| Python normal-path ownership | `pass` | No normal-path Python runtime code remains | do not reintroduce mixed-runtime ownership |
| Feature-for-feature parity | `partial` | Multiple parser, graph, query, and operator gaps remain | close all rows below or explicitly bound them |

## Operator And Runtime Contract Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| `pcg serve start` contract | `pass` | `pcg serve start` is now documented and shipped as the HTTP API convenience process, while MCP stays on `pcg mcp start` | keep command behavior and docs aligned exactly | focused Go CLI tests, docs check | validation sweep only |
| API admin route mounting | `pass` | The shipped Go API exposes the public `/api/v0/admin/*` control surface, while the runtime still mounts the shared `/healthz`, `/readyz`, `/admin/status`, and `/metrics` contract on the same listener | keep the public admin routes and runtime admin surface aligned in code, OpenAPI, and docs | `go test` for `./internal/query ./cmd/api`, OpenAPI diff, docs build | validation sweep only |
| Status endpoint breadth | `pass` | The API runtime now mounts the shared `/healthz`, `/readyz`, `/admin/status`, and `/metrics` surface, while public ingester status is available on canonical `/api/v0/status/ingesters*` routes with legacy `GET /api/v0/ingesters*` aliases | keep the mounted runtime surface and public status aliases documented and tested | `go test` for `./internal/status ./internal/query ./cmd/api`, docs build | validation sweep only |
| Run-scoped coverage endpoints | `bounded` | The shipped Go contract is checkpoint status plus repository-scoped coverage only: `/api/v0/status/index`, `/api/v0/index-status`, and `/api/v0/repositories/{repo_id}/coverage`. Run-scoped completeness routes are intentionally not part of the public OpenAPI contract on this branch | docs and OpenAPI keep that bounded contract explicit without implying `/api/v0/index-runs/*` support | OpenAPI verification, docs build | no implementation slice unless the product contract changes |
| CLI behavior breadth | `partial` | Some Go CLI flows are thinner than old Python UX | required historical flows are intentionally matched or intentionally retired | focused `go/cmd/pcg` tests and smoke checks | small follow-on slice |

## Parser And Graph-Surface Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| SQL core parsing | `pass` | Core SQL object and DDL parity is now closed: Go covers `CREATE TABLE IF NOT EXISTS`, `CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS`, `CREATE OR REPLACE FUNCTION` bodies, tagged dollar-quoted procedural bodies with `LANGUAGE` before or after `AS`, `CREATE OR REPLACE VIEW`, `CREATE PROCEDURE`, `CREATE MATERIALIZED VIEW`, legacy `EXECUTE PROCEDURE` trigger wiring, and fixture-backed `ALTER TABLE ... ADD COLUMN ...` column materialization, including bounded multi-clause `ADD COLUMN` normalization, with parser plus content/query fallback proof | keep the proof current and keep broader dialect-specific procedural forms plus wider ALTER/DDL mutation normalization documented as bounded non-goals rather than parity gaps | focused parser/content/query tests | move to validation sweep only |
| SQL/dbt lineage | `pass` | Python-era feature parity is met: the Go parser now preserves lineage through safe wrappers, lineage-preserving macro wrappers that still expose real source refs, `concat_ws`, `md5`, fixture-backed wildcard-plus-`coalesce(...)` lineage, top-level Jinja wrappers around those same safe forms, and checked-in materialization/query proof for `AnalyticsModel` plus `DataAsset` content entities. The still-unresolved dbt cases are historical Python limitations or bounded non-goals rather than missing Go parity features | compiled dbt lineage survives parse, materialization, and query proof | parser tests, materialization/query proof, broader real-repo proof as validation follow-on | move to validation sweep, not parity implementation |
| Canonical code-call materialization | `pass` | the collector emits explicit `code_call_materialization` follow-ups, the reducer owns repo-scoped retract-and-rewrite of canonical `CALLS`/`REFERENCES`/`USES_METACLASS` edges for SCIP-backed parser facts plus generic `function_calls`, Neo4j persistence stays on the canonical shared-write path, and the public Go query contract now proves exact `code/relationships`, decorator-aware `dead-code`, and `code/call-chain` traversal over persisted `CALLS` edges with exact-name or entity-id plus repo-scoped lookup instead of legacy fuzzy `CALLS_FUNCTION` assumptions. Primary non-SCIP families preserve richer generic call metadata (`full_name`, JSX/function kind where available, receiver type where available), and reducer proof covers same-file plus cross-file matching for JS/TS/TSX import-driven calls, Python module/`from` imports, exact-qualified Swift/Ruby/Elixir/PHP static-style calls, Kotlin safe-call receiver chains, PHP nullsafe receiver chains, and repo-unique cross-file bare-name calls across the remaining parser-first long-tail families (C, C++, C#, Java, Scala, Kotlin, Rust, Dart, Haskell, Perl). | required language families persist canonical call edges through parser, collector, reducer, Neo4j, and query proof without Python fallback | parser tests, collector/reducer tests, Neo4j persistence tests, query contract tests | closed |
| JavaScript graph parity | `partial` | docstrings and method-kind metadata are extracted and materialized, the graph-backed `language-query` path now returns those fields directly from graph rows, `code/search` now preserves graph-backed JavaScript metadata when the row already carries it, `entities/{entity_id}/context` now also promotes graph-backed JavaScript metadata directly into `metadata`, `semantic_summary`, `semantic_profile`, and `story`, shared query outputs emit semantic summaries and a structured `semantic_profile`, JavaScript method-kind rows now get a dedicated `javascript_method` surface kind, and repository stories now expose semantic coverage via `semantic_overview`; the remaining gap is broader graph-first promotion beyond `language-query`, `code/search`, and JavaScript entity-context/story | JS docstrings and method metadata persist and query correctly across parser, graph-backed language-query, graph-backed code/search, graph-backed JavaScript entity-context/story, content enrichment, and normal query/context surfaces | parser tests plus query proof | shared JS-family wave |
| TypeScript graph parity | `partial` | decorator and generic metadata are preserved, `TypeAlias` now persists as a first-class graph entity through the Go projector/reducer/Neo4j path, mapped types and conditional types now carry dedicated `type_alias_kind` semantics on the normal Go parser/content/query path, namespace declarations now materialize as first-class `Module` graph entities with `module_kind=namespace`, same-file declaration merging now survives the normal Go parser/content/query/story path and persists through the Go semantic-entity materialization path, the shared graph-backed `language-query` path now also projects namespace and declaration-merging metadata directly from matching graph rows, content-backed entities still participate in normal entity resolve/context when the graph is empty, the normal `code/search` fallback still searches entity names as well as source text, graph-backed `language-query`, `code/search`, `dead-code`, `code/relationships`, `code/complexity`, `entities/resolve`, and entity-context results enrich matching rows with metadata, `language-query`, `code/search`, plus entity-context emit semantic summaries and a structured `semantic_profile`, entity-context also emits a first-class `story`, and repository stories expose semantic coverage via `semantic_overview`; the remaining gap is decorator, generic, mapped-type, and conditional-type graph-native persistence beyond those shared query/story surfaces | those semantics persist and query correctly across parser, graph-backed `TypeAlias` rows, graph-backed `Module` rows, graph-backed `language-query` metadata projection, content fallback, and normal query surfaces | parser tests, reducer tests, graph tests, API/MCP proof | shared JS-family wave |
| TSX graph parity | `partial` | JSX tag usage is captured, `TypeAlias` and `Component` now persist as first-class graph entities through the Go projector/reducer/Neo4j path, TSX fragment shorthand now survives on function/component entities as `jsx_fragment_shorthand`, `as ComponentType<...>` narrowing now survives on variable entities as `component_type_assertion`, the shared graph-backed `language-query` path now also projects fragment shorthand and component-assertion metadata directly from matching graph rows, content-backed entities still participate in normal entity resolve/context when the graph is empty, the normal `code/search` fallback still searches entity names as well as source text, `code/language-query` accepts direct `tsx` requests instead of requiring the broader `typescript` alias, canonical graph writes persist JSX component usage as first-class `REFERENCES` edges, the query layer still normalizes older graph-backed `CALLS` rows with `call_kind=jsx_component` for compatibility, shared query outputs emit semantic summaries plus a structured `semantic_profile`, entity-context also emits a first-class `story`, and repository stories expose semantic coverage via `semantic_overview`; the remaining gap is `ComponentType` narrowing persistence beyond the shared graph/query surfaces | JSX component semantics now persist through parser, semantic-entity materialization, canonical graph writes, relationships, graph-backed `language-query` metadata projection, and story/query surfaces; `ComponentType` narrowing still requires follow-up | parser tests, reducer tests, graph tests, API/MCP proof | shared JS-family wave |
| Python graph parity | `partial` | decorators and async flags are extracted and materialized, type annotations are already queryable through Go content-backed APIs, identifier-assigned lambdas now materialize as function entities with `semantic_kind=lambda`, Python semantic `Function` entities carrying decorators, async flags, or lambda semantics now also persist through the semantic-entity projector/reducer/Neo4j path, Python class entities now also preserve `metaclass` metadata and persist `USES_METACLASS` relationships, content-backed entities now participate in normal entity resolve/context, the normal `code/search` fallback can now search content-backed entity names as well as source text, and the graph-backed `language-query` path now projects decorator, async, lambda, and metaclass metadata directly from graph rows while shared query/context/story surfaces continue to enrich and summarize those semantics; the remaining gap is broader dedicated graph-first modeling beyond the current function/metaclass surfaces and `language-query` step | those semantics persist and query correctly across parser, semantic-entity materialization, graph-backed language-query, content fallback for the remaining partial semantics, and normal query/context/story surfaces | parser tests, graph tests, API/MCP proof | parallel medium wave |
| Java graph parity | `pass` | applied annotation usage now persists as first-class `Annotation` graph nodes through the Go projector/reducer/Neo4j path, stays graph-first on the normal `code/language-query` surface, falls back to content when the graph is empty, and normal resolve/context/story responses still expose humanized semantic summaries plus an `applied_annotation` semantic profile | keep parser, materialization, query, and docs proof aligned | parser tests, reducer tests, Neo4j writer tests, query proof | validation sweep only |
| Kotlin graph parity | `partial` | secondary constructor metadata now reaches normal query/context semantic summaries and stories, explicit `this.` receiver calls now resolve to canonical same-file `CALLS` edges, primary constructor calls inside function bodies now materialize against class entities instead of declaration-line false positives, obvious local dotted receiver calls now carry inferred receiver type strongly enough to materialize canonical edges against class and extension-receiver functions, typed local infix calls such as `calc add 5` now emit canonical `CALLS` edges, explicit typed property, constructor-injected property, dotted property alias, local alias, chained property receivers such as `session.service.info()`, safe-call receiver chains such as `session?.service?.info()`, safe-call alias assignments such as `val provider = session?.service`, same-file function-return aliases, and bounded same-file function-return receiver chains such as `factory.createService().info()` now also materialize canonical Kotlin `CALLS` edges; package-aware sibling return lookup now reaches deeper package-root directories, but broader receiver inference is still partial | constructors and truthful receiver calls persist and query correctly across parser, reducer, content enrichment, and normal query surfaces | parser tests plus query proof | long-tail wave |
| PHP graph parity | `partial` | reducer-owned PHP call materialization now proves same-file plus cross-file static-style calls, same-class typed-property receiver calls (`$this->service->info()`) now carry inferred receiver type through the Go parser and reducer path, top-level grouped `use` imports now expand natively in Go, the comprehensive PHP fixture path now proves a dedicated static-call case end to end, and bounded local aliases from `new` expressions, `$this->property` assignments, typed property-chain aliases such as `$this->container->logger`, same-file free-function return aliases such as `$service = createService(); $service->info()`, same-file free-function return property-chain aliases such as `createFactory()->logger->info()`, direct same-file free-function return receiver chains such as `createService()->info()`, same-file free-function return call chains such as `createFactory()->createService()->info()`, declared method-return-type aliases, method-return property chains such as `$this->factory->createService()->logger`, chained static factory-return receivers such as `Factory::instance()->createService()->info()`, plus nullsafe receiver chains such as `$session?->service?->info()` now also materialize canonical object-call edges; broader object-call parity is still not yet proven end to end | static and object call edges are persisted and queryable with proof | parser tests plus end-to-end proof | long-tail wave |
| C graph parity | `pass` | typedefs now persist as first-class `Typedef` graph nodes through the Go projector/reducer/Neo4j path, prefer the graph-first normal language-query surface, fall back to content when the graph is empty, and still emit semantic summaries in normal query/context responses | keep parser, materialization, query, and docs proof aligned | parser tests, reducer tests, Neo4j writer tests, query proof | validation sweep only |
| Rust graph parity | `pass` | impl blocks now persist as `ImplBlock` graph nodes, preserve bounded lifetime metadata on function and impl signatures, are queryable through `code/language-query`, surface semantic summaries through normal `entities/resolve` and entity-context fallbacks, keep `kind`/`trait`/`target`/`semantic_summary` metadata on the normal query path, and now expose exact impl ownership through first-class graph `CONTAINS` edges to Rust functions while canonical call materialization honors exact `impl_context`-qualified Rust methods | impl ownership, bounded lifetime metadata, and relationships persist and query correctly on the normal Go query path | parser tests plus query proof | validation sweep only |
| Elixir graph parity | `pass` | guards and module attributes now persist as first-class graph-backed `Function` and `Variable` semantics through the Go semantic-entity materialization path, surface in semantic summaries, structured `semantic_profile` payloads, and repository-story semantic counts on normal query/context responses, and resolve directly through the shared graph-backed `language-query` path; `defprotocol` and `defimpl` also persist as first-class `Protocol` and `ProtocolImplementation` graph nodes, while reducer-owned same-file plus cross-file call materialization stays exact-only for qualified Elixir call metadata | Elixir guard, module-attribute, protocol, and protocol-implementation semantics persist and query correctly through parser, semantic-entity materialization, graph-backed language-query, and normal query/context/story surfaces | parser tests, reducer tests, graph tests, API/MCP proof | validation sweep only |

## IaC And Deployment Semantics Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| Terraform parity | `pass` | Python-era parser/runtime parity is met, and Go now exceeds that baseline with first-class `terraform {}` block materialization plus raw `count`/`for_each` metadata on resource rows. Provider-schema evidence is now also proven through the Go ingestion transaction: repository facts are loaded into a catalog, Terraform fact batches are matched against it, and schema-driven evidence persists to `relationship_evidence_facts` before projector work is enqueued. `count`/`for_each` expansion and `dynamic` traversal remain net-new future enhancements rather than parity blockers because Python also lacked them | Terraform structural metadata and provider-schema behavior match the Python-era platform, with query proof for the Go-only `TerraformBlock` surface and ingestion-boundary proof for schema-driven relationship evidence | parser tests, relationship tests, ingestion proof, query proof | shared HCL wave |
| Terragrunt parity | `pass` | core parsing is Go-owned, dependency/local/input entities are queryable, and `terraform.source` now also materializes through the normal `TerraformModule` surface | Terragrunt preserves its current Go-owned entities and the historical module-source semantics the Python platform exposed | parser tests, query proof, relationship tests | shared HCL wave |
| Kubernetes parity | `pass` | parser payload parity is met and now exceeds Python with `qualified_name`, and the Python-era same-name/same-namespace `Service -> Deployment` `SELECTS` heuristic now survives on the Go-owned content/query path. Real `selector` / `matchLabels` resolution and CRD schema awareness were historical Python limitations too, so they remain bounded non-goals rather than parity blockers | Kubernetes parser payloads plus the historical `SELECTS` heuristic survive parse, relationships, and query proof | parser tests, relationship tests, query proof | validation sweep only |
| ArgoCD parity | `pass` | parser payload parity is met and now exceeds Python with sync-policy normalization plus explicit ApplicationSet generator/template source split, Go emits discovery, deploy-source, and destination-platform evidence for ArgoCD Applications/ApplicationSets, the normal Go entity resolve/context fallback now surfaces that chain through semantic summaries plus synthesized `DISCOVERS_CONFIG_IN`, `DEPLOYS_FROM`, and `RUNS_ON` relationships, repository stories now expose structured deployment/gitops/support overviews for that same evidence lane, and the Go-owned `trace_deployment_chain` MCP/API path now returns story-first deployment traces with `deployment_overview`, `deployment_sources`, `cloud_resources`, `k8s_resources`, `image_refs`, `k8s_relationships`, `deployment_facts`, `deployment_fact_summary`, `controller_driven_paths`, `delivery_paths`, and concrete ArgoCD controller entities in `controller_overview.entities` | ArgoCD parser payloads plus the historical Application/ApplicationSet evidence chain survive parse, relationships, entity-context fallback, repository-story surfacing, richer controller-overview deployment-trace proof, and public query proof | parser tests, relationship tests, query proof | validation sweep only |
| CloudFormation parity | `pass` | YAML and JSON templates now share the same parser materialization, including `file_format`, cross-stack import/export buckets, first-class condition definitions, evaluated condition results when expressions are template-local, and nested-stack `template_url` metadata that now also resolves obvious repo-local nested-stack targets on the Go entity-context path | JSON and YAML templates reach the same materialization and query-proof bar for the currently modeled CloudFormation semantics | parser tests plus end-to-end proof | validation sweep only |
| Kustomize parity | `pass` | parser payload parity is met and now exceeds Python with first-class sorted `bases`, inline patch-target extraction, normalized `resource_refs`/`helm_refs`/`image_refs`, and typed Kustomize evidence for resources vs Helm vs images. The historical patch-link heuristic plus typed `DEPLOYS_FROM` query fallback now survive on the Go-owned query path | Kustomize parser payloads plus the historical typed evidence/link behavior survive parse, relationships, and normal query proof | parser tests, relationship tests, query proof | validation sweep only |
| Generic JSON | `bounded` | arbitrary JSON is intentionally quiet to avoid graph noise | accepted bounded behavior is documented and unchanged | docs build only unless scope changes | do not schedule unless requirement changes |

## Query-Surface And Documentation Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| API/MCP query surfacing | `partial` | some newly parsed semantics are not exposed through normal graph/story/context paths | each parity feature has a normal query path or an explicit non-goal | API tests, MCP proof, compose proof | pair with each feature wave |
| Language pages | `partial` | many pages still mark required features `partial` or `unsupported` | pages match implementation truth with no stale parity claims | docs build | update alongside each wave |
| Support maturity matrix | `partial` | matrix is coarser than the actual parity gap inventory | matrix and parity audit do not contradict each other | docs build | final sweep |
| Roadmap and architecture docs | `partial` | now mostly truthful, but must be kept in sync as rows close | current-state docs fully match branch truth | docs build | final sweep |

## Suggested Execution Waves

| Wave | Includes | Why this grouping works |
| --- | --- | --- |
| Wave 0 | operator/runtime contract rows | independent of parser work and unblocks cleaner operator truth |
| Wave 1 | JavaScript, TypeScript, TSX, Python | shared graph/story/context promotion work for already-materialized parser semantics; canonical SCIP-backed `CALLS` edges plus same-file and import-driven cross-file generic `function_calls` are now Go-owned for JS/TS/TSX/Python in the collector/reducer path, the reducer now also proves repo-unique cross-file bare-name resolution for the remaining parser-first long-tail families, entity-context plus repository story expose shared semantic story coverage through `story` and `semantic_overview`, and the public canonical call-graph/query proof is now closed through `code/relationships`, `dead-code`, and exact `code/call-chain` traversal over persisted `CALLS` edges. `TypeAlias`, `Typedef`, `Annotation`, and TSX `Component` are now also persisted through the semantic-entity materialization path. The remaining work is the dedicated graph-first modeling still missing for JavaScript method/docstring semantics, Python type/decorator/lambda semantics, TypeScript decorator/generic/mapped-type/conditional-type persistence, and TSX `ComponentType` narrowing semantics. |
| Wave 2 | Terraform, Terragrunt, Kubernetes, ArgoCD, CloudFormation, Kustomize | mostly validation-sweep work now that the shared IaC parser/query parity rows are Go-owned and passing; use this wave to keep docs, proof, and end-to-end validation aligned rather than reopening parser work without evidence |
| Wave 3 | Kotlin, PHP | long-tail graph promotions with lower coupling |
| Wave 4 | final query-surface proof, docs lock, full validation | closes the branch honestly |

## Approval Gate

Use this matrix to answer three questions before execution starts:

1. Which rows are required for merge on this branch?
2. Which rows can run in parallel without stepping on each other?
3. Which rows are truly bounded non-goals versus unfinished parity?

Do not treat a row as complete because the parser extracts it. A row is only
complete when persistence, query surfacing, tests, end-to-end proof, and docs
all agree.
