# Parity Closure Matrix

This matrix turns the parity audit into an execution-grade checklist.

Use this page when deciding whether a workstream is ready to split, implement,
or declare complete. A row only moves to `pass` when the Go platform matches
the old Python platform feature for feature for that surface.

## Status Legend

| Status | Meaning |
| --- | --- |
| `pass` | Go-owned and parity-complete |
| `partial` | Go-owned, but feature or operator parity is still incomplete |
| `fail` | required parity behavior is still missing |
| `bounded` | intentionally narrower than Python and must stay documented as a non-goal |

## Branch Baseline

| Workstream | Status | Current truth | Approval bar |
| --- | --- | --- | --- |
| Runtime ownership | `pass` | Long-running and one-shot services are Go-owned end to end | keep Go-only ownership intact |
| Deployment ownership | `pass` | Dockerfile, Compose, and Helm run the Go platform | keep docs and validation aligned |
| Python normal-path ownership | `pass` | No normal-path Python runtime code remains | do not reintroduce mixed-runtime ownership |
| Feature-for-feature parity | `partial` | Multiple parser, graph, query, and operator gaps remain | close all rows below or explicitly bound them |

## Operator And Runtime Contract Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| `pcg serve start` contract | `pass` | `pcg serve start` is now documented and shipped as the HTTP API convenience process, while MCP stays on `pcg mcp start` | keep command behavior and docs aligned exactly | focused Go CLI tests, docs check | validation sweep only |
| API admin route mounting | `partial` | Go admin handlers exist but are not fully exposed through shipped API wiring | admin routes are either fully mounted or fully removed from contract/docs | `go test` for `go/cmd/api` and `go/internal/query`, OpenAPI diff | standalone small slice |
| Status endpoint breadth | `partial` | Current status surface is narrower than parts of the historical operator model | final status surface is intentional, documented, and tested | query/status tests plus compose proof | pair with admin route slice |
| Run-scoped coverage endpoints | `partial` | The Go API now ships `/api/v0/status/index` plus the legacy `/api/v0/index-status` alias, but true run-scoped completeness endpoints are still not ported; only repository-scoped coverage is available today | run-scoped endpoints exist and are tested, or the parity gap stays explicitly documented without overclaiming the public contract | OpenAPI verification, docs build | pair with API/admin slice |
| CLI behavior breadth | `partial` | Some Go CLI flows are thinner than old Python UX | required historical flows are intentionally matched or intentionally retired | focused `go/cmd/pcg` tests and smoke checks | small follow-on slice |

## Parser And Graph-Surface Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| SQL core parsing | `partial` | Mostly complete, with checked-in proof for `CREATE OR REPLACE FUNCTION` bodies, legacy `EXECUTE PROCEDURE` trigger wiring, and fixture-backed `ALTER TABLE ... ADD COLUMN ...` column materialization, but some procedural SQL and broader DDL edges may still remain | no documented SQL-core parity gaps remain | focused parser tests | fold into SQL/dbt wave |
| SQL/dbt lineage | `pass` | Python-era feature parity is met: the Go parser now preserves lineage through safe wrappers, lineage-preserving macro wrappers that still expose real source refs, `concat_ws`, `md5`, fixture-backed wildcard-plus-`coalesce(...)` lineage, top-level Jinja wrappers around those same safe forms, and checked-in materialization/query proof for `AnalyticsModel` plus `DataAsset` content entities. The still-unresolved dbt cases are historical Python limitations or bounded non-goals rather than missing Go parity features | compiled dbt lineage survives parse, materialization, and query proof | parser tests, materialization/query proof, broader real-repo proof as validation follow-on | move to validation sweep, not parity implementation |
| Canonical code-call materialization | `partial` | the collector and reducer now own canonical `CALLS` edge writes for SCIP-backed parser facts plus generic `function_calls`, including repo-scoped retraction and rewrite during normal Go ingestion. Primary non-SCIP families now preserve richer generic call metadata (`full_name`, JSX/function kind where available, receiver type where available), and the reducer now proves safe same-file plus cross-file matching for JS/TS/TSX import-driven calls, Python module/`from` imports, exact-qualified Swift/Ruby/Elixir/PHP static-style calls, and repo-unique cross-file bare-name calls across the remaining parser-first long-tail families (C, C++, C#, Java, Scala, Kotlin, Rust, Dart, Haskell, Perl). `code/relationships` name-based graph lookup, `code/call-chain` request wiring, and `dead-code` decorator exclusions are now proven through the Go public contract. The remaining gap is broader end-to-end proof and language-family coverage across those surfaces, not Python fallback. | required language families persist canonical call edges through parser, collector, reducer, and query proof without Python fallback | parser tests, reducer/collector tests, query proof | pair with parser-family waves |
| JavaScript graph parity | `partial` | docstrings and method-kind metadata are extracted and materialized, graph-backed query surfaces enrich matching rows with that metadata, `language-query`, `code/search`, plus entity-context emit semantic summaries and a structured `semantic_profile`, entity-context now also emits a first-class `story`, and repository stories now expose semantic coverage via `semantic_overview`; the remaining gap is dedicated graph-first promotion beyond those shared query/story surfaces | JS docstrings and method metadata persist and query correctly across parser, content, and normal query/context surfaces | parser tests plus query proof | shared JS-family wave |
| TypeScript graph parity | `partial` | decorator and generic metadata are preserved, type aliases are already queryable through Go content-backed APIs, content-backed entities now participate in normal entity resolve/context, the normal `code/search` fallback can now search content-backed entity names as well as source text, graph-backed `language-query`, `code/search`, `dead-code`, `code/relationships`, `code/complexity`, `entities/resolve`, and entity-context results now enrich matching rows with metadata, `language-query`, `code/search`, plus entity-context emit semantic summaries and a structured `semantic_profile`, entity-context now also emits a first-class `story`, and repository stories now expose semantic coverage via `semantic_overview`; the remaining gap is dedicated graph-first modeling beyond those shared query/story surfaces | those semantics persist and query correctly across parser, content, and normal query surfaces | parser tests, graph tests, API/MCP proof | shared JS-family wave |
| TSX graph parity | `partial` | JSX tag usage is captured, type aliases plus component semantics are queryable through Go content-backed APIs, content-backed entities now participate in normal entity resolve/context, the normal `code/search` fallback can now search content-backed entity names as well as source text, `code/language-query` now also accepts direct `tsx` requests instead of requiring the broader `typescript` alias, normal `code/relationships` can synthesize JSX component `REFERENCES` edges for content-backed entities, shared query outputs emit semantic summaries plus a structured `semantic_profile`, entity-context now also emits a first-class `story`, and repository stories now expose semantic coverage via `semantic_overview`; the remaining gap is full graph-first component/reference modeling | JSX component semantics become first-class across parser, content, relationships, and story/query surfaces | parser tests, graph tests, API/MCP proof | shared JS-family wave |
| Python graph parity | `partial` | decorators and async flags are extracted and materialized, type annotations are already queryable through Go content-backed APIs, content-backed entities now participate in normal entity resolve/context, the normal `code/search` fallback can now search content-backed entity names as well as source text, graph-backed query surfaces enrich matching rows with metadata, `language-query`, `code/search`, plus entity-context emit semantic summaries and a structured `semantic_profile`, entity-context now also emits a first-class `story`, and repository stories now expose semantic coverage via `semantic_overview`; the remaining gap is dedicated graph-first modeling beyond those shared query/story surfaces | those semantics persist and query correctly across parser, content, and normal query/context surfaces | parser tests, graph tests, API/MCP proof | parallel medium wave |
| Java graph parity | `partial` | applied annotation usage is now queryable through content-backed `Annotation` entities and normal resolve/context/story responses, including humanized semantic summaries and an `applied_annotation` semantic profile, but it is still not first-class on the persisted graph | annotations persist and query correctly across parser, content, and normal query surfaces | parser tests plus query proof | long-tail wave |
| Kotlin graph parity | `partial` | secondary constructor metadata now reaches normal query/context semantic summaries, explicit `this.` receiver calls now resolve to canonical same-file `CALLS` edges, primary constructor calls inside function bodies now materialize against class entities instead of declaration-line false positives, obvious local dotted receiver calls now carry inferred receiver type strongly enough to materialize canonical edges against class and extension-receiver functions, typed local infix calls such as `calc add 5` now emit canonical `CALLS` edges, and explicit typed property, local alias, plus chained property receivers such as `session.service.info()` now also materialize canonical Kotlin `CALLS` edges; broader receiver inference and graph/story promotion are still partial | constructors and truthful receiver calls persist and query correctly across parser, reducer, content enrichment, and normal query surfaces | parser tests plus query proof | long-tail wave |
| PHP graph parity | `partial` | reducer-owned PHP call materialization now proves same-file plus cross-file static-style calls, same-class typed-property receiver calls (`$this->service->info()`) now carry inferred receiver type through the Go parser and reducer path, top-level grouped `use` imports now expand natively in Go, the comprehensive PHP fixture path now proves a dedicated static-call case end to end, and bounded local aliases from `new` expressions, `$this->property` assignments, plus typed property-chain aliases such as `$this->container->logger` now also materialize canonical object-call edges; broader object-call parity is still not yet proven end to end | static and object call edges are persisted and queryable with proof | parser tests plus end-to-end proof | long-tail wave |
| C graph parity | `partial` | typedefs are queryable through the Go content-backed language-query surface and now emit semantic summaries in normal query/context responses, but they are not first-class graph entities yet | typedef semantics persist, query correctly, and become graph-first where parity requires it | parser tests plus query proof | long-tail wave |
| Rust graph parity | `partial` | impl blocks now persist as `ImplBlock` content entities, preserve bounded lifetime metadata on function and impl signatures, are queryable through `code/language-query`, surface semantic summaries through normal `entities/resolve` and entity-context fallbacks, and now expose exact impl ownership through normal `code/relationships` content fallback, while canonical call materialization honors exact `impl_context`-qualified Rust methods; implementation edges are still not first-class on the graph | impl ownership, bounded lifetime metadata, and relationships persist and query correctly on the normal Go query path | parser tests plus query proof | long-tail wave |
| Elixir graph parity | `partial` | guards, protocols, implementations, and module attributes now surface in semantic summaries on normal query/context responses, and the normal Go query path can resolve `guard`, `protocol_implementation`, and `module_attribute` as semantic aliases over flattened `Function`, `Module`, and `Variable` rows; `defprotocol` now materializes as a first-class `Protocol` content entity on the normal Go parser/content/query path, and reducer-owned same-file plus cross-file call materialization stays exact-only for qualified Elixir call metadata, but those semantics are still flattened into generic module/function/variable records on the persisted graph | those semantics persist and query correctly on the normal Go query path | parser tests plus query proof | long-tail wave |

## IaC And Deployment Semantics Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| Terraform parity | `pass` | Python-era parser/runtime parity is met, and Go now exceeds that baseline with first-class `terraform {}` block materialization. `count`/`for_each` expansion and `dynamic` traversal remain net-new future enhancements rather than parity blockers because Python also lacked them | Terraform structural metadata and provider-schema behavior match the Python-era platform, with query proof for the Go-only `TerraformBlock` surface | parser tests, relationship tests, query proof | shared HCL wave |
| Terragrunt parity | `pass` | core parsing is Go-owned, dependency/local/input entities are queryable, and `terraform.source` now also materializes through the normal `TerraformModule` surface | Terragrunt preserves its current Go-owned entities and the historical module-source semantics the Python platform exposed | parser tests, query proof, relationship tests | shared HCL wave |
| Kubernetes parity | `pass` | parser payload parity is met and now exceeds Python with `qualified_name`, and the Python-era same-name/same-namespace `Service -> Deployment` `SELECTS` heuristic now survives on the Go-owned content/query path. Real `selector` / `matchLabels` resolution and CRD schema awareness were historical Python limitations too, so they remain bounded non-goals rather than parity blockers | Kubernetes parser payloads plus the historical `SELECTS` heuristic survive parse, relationships, and query proof | parser tests, relationship tests, query proof | validation sweep only |
| ArgoCD parity | `pass` | parser payload parity is met and now exceeds Python with sync-policy normalization plus explicit ApplicationSet generator/template source split, Go emits discovery, deploy-source, and destination-platform evidence for ArgoCD Applications/ApplicationSets, the normal Go entity resolve/context fallback now surfaces that chain through semantic summaries plus synthesized `DISCOVERS_CONFIG_IN`, `DEPLOYS_FROM`, and `RUNS_ON` relationships, repository stories now expose structured deployment/gitops/support overviews for that same evidence lane, and the Go-owned `trace_deployment_chain` MCP/API path now returns story-first deployment traces with `deployment_overview`, `deployment_sources`, `cloud_resources`, `k8s_resources`, `image_refs`, `k8s_relationships`, `deployment_facts`, `deployment_fact_summary`, `controller_driven_paths`, `delivery_paths`, and concrete ArgoCD controller entities in `controller_overview.entities` | ArgoCD parser payloads plus the historical Application/ApplicationSet evidence chain survive parse, relationships, entity-context fallback, repository-story surfacing, richer controller-overview deployment-trace proof, and public query proof | parser tests, relationship tests, query proof | validation sweep only |
| CloudFormation parity | `pass` | YAML and JSON templates now share the same parser materialization, including `file_format`, cross-stack import/export buckets, first-class condition definitions, evaluated condition results when expressions are template-local, and nested-stack `template_url` metadata that now also resolves obvious repo-local nested-stack targets on the Go entity-context path | JSON and YAML templates reach the same materialization and query-proof bar for the currently modeled CloudFormation semantics | parser tests plus end-to-end proof | validation sweep only |
| Kustomize parity | `pass` | parser payload parity is met and now exceeds Python with first-class sorted `bases`, inline patch-target extraction, normalized `resource_refs`/`helm_refs`/`image_refs`, and typed Kustomize evidence for resources vs Helm vs images. The historical patch-link heuristic plus typed `DEPLOYS_FROM` query fallback now survive on the Go-owned query path | Kustomize parser payloads plus the historical typed evidence/link behavior survive parse, relationships, and normal query proof | parser tests, relationship tests, query proof | validation sweep only |
| Generic JSON | `bounded` | arbitrary JSON is intentionally quiet to avoid graph noise | accepted bounded behavior is documented and unchanged | docs build only unless scope changes | do not schedule unless requirement changes |

## Query-Surface And Documentation Matrix

| Workstream | Status | Gap now | Done means | Validation gate | Recommended split |
| --- | --- | --- | --- | --- | --- |
| API/MCP query surfacing | `partial` | some newly parsed semantics are not exposed through normal graph/story/context paths | each parity feature has a normal query path or an explicit non-goal | API tests, MCP proof, compose proof | pair with each feature wave |
| Language pages | `partial` | many pages still mark required features `partial` or `unsupported` | pages match implementation truth with no stale parity claims | docs build | update alongside each wave |
| Support maturity matrix | `partial` | matrix is coarser than the actual parity gap inventory | matrix and parity audit do not contradict each other | docs build | final sweep |
| Roadmap and architecture docs | `partial` | now mostly truthful, but must be kept in sync as rows close | current-state docs fully match branch truth | docs build | final sweep |

## Suggested Execution Waves

| Wave | Includes | Why this grouping works |
| --- | --- | --- |
| Wave 0 | operator/runtime contract rows | independent of parser work and unblocks cleaner operator truth |
| Wave 1 | JavaScript, TypeScript, TSX, Python | shared graph/story/context promotion work for already-materialized parser semantics; canonical SCIP-backed `CALLS` edges plus same-file and import-driven cross-file generic `function_calls` are now Go-owned for JS/TS/TSX/Python in the collector/reducer path, the reducer now also proves repo-unique cross-file bare-name resolution for the remaining parser-first long-tail families, and entity-context plus repository story now expose shared semantic story coverage through `story` and `semantic_overview`. The remaining work is public call-graph/query proof plus the dedicated graph-first modeling still missing for JS/TS/TSX/Python entities. Normal `language-query`, `code/search`, `dead-code`, `code/relationships`, `code/complexity`, `entities/resolve`, and entity-context metadata enrichment are now in place for graph-backed JS/TS/Python entities, `language-query` and `code/search` now also emit semantic summaries for matching graph-backed semantics, while type aliases, type annotations, and components are now also available through content-backed entity resolve/context surfaces, content-backed `code/search` name fallback, semantic-summary fallback for content-backed entities, story fallback for matching semantic entities, and TSX component references can now surface through content-backed `code/relationships` fallback |
| Wave 2 | Terraform, Terragrunt, Kubernetes, ArgoCD, CloudFormation, Kustomize | mostly validation-sweep work now that the shared IaC parser/query parity rows are Go-owned and passing; use this wave to keep docs, proof, and end-to-end validation aligned rather than reopening parser work without evidence |
| Wave 3 | Java, Kotlin, PHP, C, Rust, Elixir | long-tail graph promotions with lower coupling |
| Wave 4 | final query-surface proof, docs lock, full validation | closes the branch honestly |

## Approval Gate

Use this matrix to answer three questions before execution starts:

1. Which rows are required for merge on this branch?
2. Which rows can run in parallel without stepping on each other?
3. Which rows are truly bounded non-goals versus unfinished parity?

Do not treat a row as complete because the parser extracts it. A row is only
complete when persistence, query surfacing, tests, end-to-end proof, and docs
all agree.
