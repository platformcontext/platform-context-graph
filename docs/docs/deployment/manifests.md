# Minimal manifests

The raw manifest example moved into the Kubernetes lane:
[Minimal Manifests](../deploy/kubernetes/manifests.md).

Use it for a compact API runtime example. Use
[Helm Quickstart](../deploy/kubernetes/helm-quickstart.md) for the supported
split-service Kubernetes deployment.
