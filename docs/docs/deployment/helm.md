# Helm deployment

The Helm guide moved into the Kubernetes lane.

- Use [Helm Quickstart](../deploy/kubernetes/helm-quickstart.md) for install
  commands and rollout checks.
- Use [Helm Values](../deploy/kubernetes/helm-values.md) for the chart value
  map.
- Use [Storage](../deploy/kubernetes/storage.md) before setting database and
  graph values.
