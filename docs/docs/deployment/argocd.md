# Argo CD deployment

The Argo CD guide moved into the Kubernetes lane:
[Argo CD and GitOps](../deploy/kubernetes/argocd.md).

Use [Helm Values](../deploy/kubernetes/helm-values.md) when reviewing the
values files under `deploy/argocd`.
