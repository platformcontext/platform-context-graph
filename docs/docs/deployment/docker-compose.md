# Docker Compose

This legacy deployment page now points to the local Docker Compose guide:

[Run Locally: Docker Compose](../run-locally/docker-compose.md)

Use that page for the current local commands, the NornicDB default stack, the
explicit Neo4j stack, and the local API/MCP ports.

For operator verification, metrics checks, and compose-backed test wrappers,
use [Local Testing](../reference/local-testing.md).
