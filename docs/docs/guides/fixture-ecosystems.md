# Fixture Ecosystems

PlatformContextGraph includes fixture ecosystems that support deployable-service testing and docs examples.

Fixture root:
- `tests/fixtures/ecosystems`

Current fixture categories:
- code-only Python
- Python + Terraform
- Python + Crossplane
- Helm + Argo CD
- shared infrastructure platform repo

These fixtures are intended to back:
- local Docker Compose indexing flows
- HTTP API and MCP integration tests
- shared-infra walkthroughs
