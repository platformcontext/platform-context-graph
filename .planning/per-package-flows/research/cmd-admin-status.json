{
  "package": "go/cmd/admin-status",
  "role": "One-shot CLI that renders the PCG status report from Postgres in text or JSON.",
  "files": ["main.go", "doc.go"],
  "key_identifiers": {
    "functions": ["run", "renderStatus"],
    "constants": ["defaultFormat"],
    "external_deps": ["runtimecfg.OpenPostgres", "postgres.NewStatusStore", "postgres.SQLQueryer", "statuspkg.LoadReport", "statuspkg.RenderText", "statuspkg.RenderJSON", "statuspkg.DefaultOptions", "statuspkg.Reader"]
  },
  "invariants": [
    "One-shot: exits after printing the report; no long-running loop",
    "Unknown --format fails with unsupported format error (main.go:86)",
    "No OTEL providers registered; logging via stdlib log",
    "Report time is time.Now().UTC() — no cache layer (main.go:49)"
  ],
  "configuration": {
    "flags": ["--format (text|json)"],
    "env": ["PCG_POSTGRES_DSN via runtime.OpenPostgres"]
  },
  "failure_modes": [
    "Postgres DSN missing or wrong: binary exits non-zero before calling renderStatus",
    "Unknown format string: exits with 'unsupported format' error"
  ]
}
