services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_ENV: production
      PORT: "8080"
    depends_on:
      - database

  database:
    image: postgres:16
    ports:
      - "5432:5432"
