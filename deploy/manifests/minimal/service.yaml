apiVersion: v1
kind: Service
metadata:
  name: platform-context-graph
  namespace: platform-context-graph
spec:
  type: ClusterIP
  selector:
    app: platform-context-graph
  ports:
    - name: http
      port: 8080
      targetPort: http
